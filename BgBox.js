import React from "react";
import { MotionConfig, motion } from "framer-motion";
const BgBox = () => {
  return (
    <div>
      <div class="absolute inset-0 flex overflow-hidden ">
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
        <motion.div className="">
          {/* <span className="text-[#efe1f8]"></span> */}
          <MotionConfig>
            {[...Array(8)].map((_, index) => (
              <motion.div
                initial={{
                  rotate: "45deg",
                }}
                animate={{
                  rotate: "45deg",
                }}
                key={index}
                className="border border-gray-200 w-10 h-10 grid place-content-center"
              ></motion.div>
            ))}
          </MotionConfig>
        </motion.div>
      </div>
    </div>
  );
};

export default BgBox;
